local branches branch list

branches=$(git branch -vv -a)
branch=$(echo "$branches" | fzf -m)
list=$(echo "$branch" | awk '{print $1}' | sed "s/.* //" | tr "\n" " ")

BUFFER="$BUFFER $list"

CURSOR=${#BUFFER}
