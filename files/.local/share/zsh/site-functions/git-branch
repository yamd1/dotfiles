local branches branch
branches=$(git branch -vv -a) &&
branch=$(echo "$branches" | fzf -m) &&
for item in "$branch" ; do
  BUFFER="${BUFFER} $(echo "$item" | awk '{print $1}' | sed "s/.* //" | sed "s/\n/\ /")"
done

CURSOR=${#BUFFER}
